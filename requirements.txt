fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pyyaml>=6.0
python-dotenv>=1.0
httpx>=0.27
python-multipart>=0.0.9
elevenlabs>=2.0
openai>=1.35
# Optional, only needed for live audio (voice intake / human-in-the-loop / --listen):
# pyaudio>=0.2.14   (macOS: `brew install portaudio` first)
